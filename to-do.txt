bugs
    position footer total relative to parent? so it doesn't move on "bounce"

validations
    wash out button color until user can proceed

stretch: expanded view
    show zoomed in view of picture, desc, etc.
    add ons/modifications (creamer, sugar, etc)
    "add to cart" button with dropdown for quantity
    if already in cart, allow to delete or edit quantity
    add "go to cart" button
    see drawing

desktop styling
    use original mobile design, just bigger
    add tagline to header
    add Java Script Rx logo to popup at end

tablet styling
    use mobile design, but add description preview and quantity dropdown
    add tagline to header